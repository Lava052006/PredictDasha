# Copy this file to .env.local for local overrides
# In production (Vercel), set this in the Vercel Dashboard → Settings → Environment Variables

# Replace with your actual Render backend URL after deploying
VITE_API_BASE_URL=https://predictdasha-api.onrender.com
